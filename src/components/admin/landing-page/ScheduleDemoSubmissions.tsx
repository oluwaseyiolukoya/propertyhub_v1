import React from 'react';
import { FormSubmissions } from './FormSubmissions';

export function ScheduleDemoSubmissions() {
  return (
    <FormSubmissions
      formType="schedule_demo"
      title="Demo Requests"
      description="Manage and schedule product demonstrations"
    />
  );
}

