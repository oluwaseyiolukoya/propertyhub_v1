import React from 'react';
import { FormSubmissions } from './FormSubmissions';

export function ContactFormSubmissions() {
  return (
    <FormSubmissions
      formType="contact_us"
      title="Contact Form Submissions"
      description="Manage and respond to contact inquiries"
    />
  );
}

